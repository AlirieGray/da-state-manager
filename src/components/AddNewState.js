import plus from '../images/plus.png'
import '../styles/addNewState.css'

function AddNewState() {
 
  return (
    <div>
        <img className="addButton" src={plus} />
        Add New State
    </div>
  );
}

export default AddNewState
